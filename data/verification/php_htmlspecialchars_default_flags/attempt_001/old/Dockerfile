FROM php:8.3-cli
WORKDIR /case
# Candidate: PHP-12-htmlspecialchars-default-flags
# Side: old
# Intended dependency: php-core:8.0.30
# Offline package paths used by local runner: data\verification\php_core\php80
# Run the configured client with include_path pointing at local package roots.
