FROM golang:1.22
WORKDIR /case
# Candidate: go-timer-channel-capacity
# Side: new
# Intended dependency: go-stdlib-time:1.23
# Offline package paths used by local runner: data\verification\go_timer_channel\new
# Run the configured client with GO_ADAPTER_PACKAGE_PATHS pointing at local package roots.
